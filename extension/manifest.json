{
  "manifest_version": 3,
  "name": "Berlin Lost & Found Form Helper",
  "version": "0.1.0",
  "description": "Fills reviewed lost-property fields. Only separately approved reviewed_submit adapters can offer submission after repeated user confirmation.",
  "permissions": ["activeTab", "scripting", "storage", "clipboardWrite"],
  "action": {
    "default_title": "Fill lost-property form",
    "default_popup": "popup.html"
  }
}
